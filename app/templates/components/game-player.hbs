{{#if gameStarted}}
  <div class="game-container">
    {{#player-speach as |playerSpeach npcSpeach speakClear npcSpeakClear|}}
      {{#scene-scripts 
        playerSpeach=(action playerSpeach)
        npcSpeach=(action npcSpeach)
        speakClear=(action speakClear)
        npcSpeakClear=(action npcSpeakClear)
        as |scripts scene previousScene convo convoOption|
      }}
        {{#player-actions
          scene=scene
          previousScene=previousScene
          scripts=scripts
          componentName=componentName
          playerSpeach=(action playerSpeach)
          speakClear=(action speakClear)
          convo=(action convo)
          as |walk thingClicked helper clearHelper verb helperText changeScene|
        }}
          {{component componentName
            scene=scene
            walk=(action walk)
            thingClicked=(action thingClicked)
            changeScene=(action changeScene)
            helper=(action helper)
            clearHelper=(action clearHelper)
            convoOption=(action convoOption)
            playerSpeach=(action playerSpeach)
            npcSpeach=(action npcSpeach)
            verb=verb
          }}
          {{menu-area
            componentName=componentName
            scene=scene 
            verb=verb
            helperText=helperText
            scripts=scripts 
            playerSpeach=(action playerSpeach)
            helper=(action helper)
            clearHelper=(action clearHelper)
          }}
        {{/player-actions}}
      {{/scene-scripts}}
    {{/player-speach}}
  </div>
{{else}}
  {{start-screen gameStarted=gameStarted}}
{{/if}}